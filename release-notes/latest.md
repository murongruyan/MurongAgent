# MurongAgent 1.37

## 1.37 / 26080301 — 2026-08-03

本文件只记录当前待发布版本 1.37 的更新说明，不包含已经发布版本的历史变更。

### Added

- **双端离线朗读与语音输入**：Android 与 Desktop 均可安装离线中文朗读模型（MeloTTS，下载约 159 MB）与流式语音识别模型（Zipformer 中英标点版，下载约 128 MB）；完全本地运行，不依赖系统语音引擎，不上传任何音频。助手回复旁新增朗读按钮，输入框旁新增麦克风按钮。
- **计划模式重做**：复杂任务会自动先生成完整执行计划；计划卡片始终内联在聊天中并默认收起成小卡片，不再用全屏弹窗遮挡模型输出。用户先验收计划，可点“修改计划”让模型按反馈重新生成，确认后再点“执行”。
- **目标模式闭环**：顶栏大标签改为紧凑目标条，可编辑、暂停、恢复、完成；模型能通过 `[goal:start]`、`[goal:update]`、`[goal:pause]`、`[goal:resume]`、`[goal:complete]`、`[plan:start]` 控制标记自主开启 / 更新 / 暂停 / 恢复 / 完成目标和请求计划。说“开个目标模式… / 目标模式开始干吧”会自动进入目标模式。
- **项目页新建文件与新建文件夹**：项目文件树工具栏新增“新建文件”“新建文件夹”按钮，带名称校验，创建后自动刷新目录、展开并打开新文件。
- **扩展环境开发工具自愈**：终端扩展环境缺 python / pip / git / clang 等常用开发命令时，会自动 `pkg install` 安装并重试一次，失败时给出明确提示；PRoot 版本化 symlink（python → python3.14）在非交互 shell 中也能正确解析。
- **系统环境接入真实 Termux 工具**：设备安装了 Termux 时，其 `bin` 目录会自动追加到系统 PATH，python / pip / clang 等可直接被 Agent 与编辑器使用。
- **桌面端语音设置页**：新增“朗读与语音输入”设置卡片，可安装 / 删除 TTS 与 ASR 模型，显示运行时与模型状态、SHA-256 与安全说明。
- **端侧中文屏幕 OCR**：Phone Agent 新增随 APK 打包的中文 OCR 感知层；微信等应用不提供无障碍语义树时，仍能在手机本地识别文字和控件中心坐标，截图与识别结果不上传。
- **Nuphus 电脑控制预设**：Desktop MCP 设置可一键添加 Nuphus；默认关闭自动启动，写操作启用严格确认，仅信任屏幕尺寸、窗口、视觉/OCR 和浏览器读取类工具。
- **可交互离屏查看器**：手机操作可在独立页面中查看并触控离屏任务；查看器提供“返回聊天 / 回到语音助手 / 全屏预览 / 全屏接管 / 关闭离屏”。ColorOS 流体云按真机可见上限固定为“返回聊天 / 回到语音助手 / 离屏控制”三个入口，点“离屏控制”或卡片本身进入查看器后再选择预览、接管或关闭。聊天页的离屏任务卡片直接嵌入持续刷新的小型实时画面，点击画面可放大全屏预览，同时提供“回到语音助手 / 全屏预览 / 全屏接管 / 关闭离屏”。内嵌画面复用同一个虚拟 Display，不创建第二个 Display、不重启目标应用。“全屏预览”保留隔离显示，原机分辨率下 1:1 显示，返回手势映射到离屏应用；“全屏接管”把当前任务栈迁回物理主屏原生渲染。悬浮窗还可最小化为小点后再恢复。
- **语音助手输入布局**：修复输入法弹出后固定高度与 IME inset 叠加，导致聊天输入框被压扁、已输入文字不可见的问题；键盘显示时弹窗会使用完整可用高度并收起非关键说明，输入区保持至少两行可读空间。
- **离屏画质设置**：“设置 → 手机操作”新增分辨率、目标刷新率、JPEG 质量和目标码率选项，支持原机分辨率与跟随屏幕刷新率，并在普通查看模式中同时显示实际帧率和目标帧率。

### Changed

- **模型切换修复**：切换聊天框下方或设置页中的模型时，会同步更新活动中继的 model，不再出现“切换了但实际请求仍是旧模型”的情况。
- **Android 朗读体验**：恢复流式合成边生成边播放，点击后首声明显更快、句间不再停顿；release 压缩下通过 keep 规则保留流式回调的 JNI 反射签名，修复 release 包点朗读即闪退的问题。
- **桌面端朗读范围**：朗读只读助手回复正文，不再把“朗读 / 从此处分叉 / 回退到此处”等操作按钮文字一并读出来。
- **桌面端麦克风交互**：麦克风按钮移到发送按钮旁，点击开始、再点结束并转文字，不再需要长按；静音约 2.5 秒自动停止；识别结果自动填入输入框。
- **朗读模型校验**：SHA-256 以官方固定值校验，官方重新打包时会回退到官方 checksum 清单，不再因单一硬编码值卡死安装。
- **发布流程**：GitHub Release 资产上传改经 origin、Release 同步只同步资产元数据，修复部分环境上传不稳定。
- **Root 离屏链路**：已 Root 设备直接通过 UID 0 的 `app_process` 承载离屏显示，Shizuku / Sui 仅在无 Root 或 Root 直连失败时兜底；强制停止 Murong 后，Root 辅助进程会自动退出，不残留后台。

### Fixed

- 修复手机聊天页把同一图片的不同缓存副本重复列出、并在输入框上方堆成多行遮挡聊天的问题；最近图片现在按文件特征去重，默认只显示一条紧凑入口，需要时再展开选择和预览。
- 修复模型接口返回的原始 HTTP 400 请求体直接显示给用户的问题；图片能力不兼容、限流、服务异常和网络失败现在显示可操作的短提示，原始诊断只保留在内部错误记录中。
- 修复“最终收口校验 / 已记录到工具审计”等内部治理消息进入助手聊天气泡的问题；收口拦截和工具审计仍完整持久化并可在工具页查看，但不再向普通用户暴露内部协议。
- 修复桌面计划模式同时显示模型计划和第二个大型计划面板、三级说明被重复拆成额外步骤、完成后仍需手动清面板的问题；计划只保留在原助手消息下方，提供“按计划执行 / 修改计划 / 取消计划”，执行时输入框上方仅显示进度胶囊，悬停才展开完整计划。手机端“修改计划”也改为卡片内短输入区，不再弹出遮挡视野的大对话框。
- 修复桌面“+”文件选择器只能选择项目第一层文件的问题；现在可逐级进入子文件夹、返回上级、递归搜索，并可分别选择文件或整个文件夹作为本轮上下文。
- 修复桌面会话右键菜单缺少 Markdown 保存入口的问题；现在可直接导出 JSON、Markdown 或跨端 JSON，导出时自动脱敏 GitHub / API Token、Authorization、密码和 secret 字段。
- 修复桌面终端命令超时或非零退出仍被记为成功、Git 上传 / rebase 因凭据或编辑器提示无限等待的问题；真实退出码和超时会交回模型继续纠错，Git 运行默认禁用交互式凭据与编辑器提示。
- 修复桌面底栏只显示缓存 Token 数、输入框缺少上下文占用反馈的问题；底栏新增缓存命中占比，输入框下方新增上下文圆环，悬停可查看已用 / 上限 Token 与缓存明细。
- 修复 Windows 桌面端打包时把 `build/vlm` 的旧 VLM 编译缓存误当成 Go 源包扫描、导致 Wails 构建被 FlatBuffers 示例代码阻断的问题；VLM 中间产物现在写入外部缓存目录，不再污染主模块包扫描。
- 修复手机端计划确认页无法随长输入滚动、底部“执行计划”按钮被挤出屏幕的问题。
- 修复 Phone Agent 的模型自然语言只显示最后一句、后一句覆盖前一句的问题；同一次推理会按流式增量更新，后续推理步骤追加保留，聊天卡片不再用预设心跳文案冒充模型输出。
- 修复默认最小化的手机操作胶囊无法点击展开的问题；现在能正确区分点击和拖动，支持展开、收起，并可在拖到新位置后再次展开。任务完成后仍保留“待确认”供用户验收。
- 修复完成态流体云把第三按钮误换成“确认并关闭”的问题；只要离屏仍可验收，就固定显示“返回聊天 / 回到语音助手 / 离屏控制”，关闭入口保留在查看器和聊天卡片中。
- 修复“打开微信并发送消息”等复合手机指令先截图识别再启动应用的问题；现在先通过通用、限时的 Android Launcher 直接启动目标应用，再进入后续读屏与操作，不硬编码应用 Activity 或点击坐标。
- 修复打开抖音后误点“关注”的问题：搜索任务会通过 Android 已验证并绑定抖音包的应用内搜索入口直达结果页，不再让 OCR 用顶部任意文字猜搜索按钮；用户没有明确要求时，模型禁止点击“关注 / 订阅 / 加好友”，进入账号改点头像或用户名，查最新作品会跳过“置顶”。
- 修复模型把拼多多首页误判为商品详情页后错误返回，或明知底栏有“首页”仍重复点击的问题；底部“首页 / 多多视频 / 聊天 / 个人中心”等 OCR 导航现在作为强页面锚点，只读页面询问会直接回答并拦截多余点击。
- 补全本地 Phone Agent 的动作 JSON 协议并兼容中文动作别名，同时避免本地系统提示被截断，显著减少“模式动作格式无效”与纠正失败。
- 修复 Phone Agent 对明确指定收件人和正文的普通消息仍错误拒绝的问题；该单条消息可按用户指令发送，但仍禁止擅改对象或内容、群发、支付和下单。
- 为小型本地视觉模型提供精简的单动作协议，兼容文本形式的 `phone_action(...)` / `phone_finish(...)`，并限制无效输出长度与重试次数；失败时会显示过滤内部推理后的模型可见回复摘要，不再只显示笼统格式错误。
- 修复手机操作进度悬浮窗被一并截给视觉模型、遮住真实界面并导致误点的问题；截屏时会短暂隐藏 Murong 自身悬浮窗，完成后立即恢复显示。
- 修复无障碍无法取得第三方应用编辑节点时，Root `input text` 对中文静默失效却仍报告成功的问题；现在会通过 Unicode 剪贴板粘贴输入，并在完成后恢复用户原剪贴板。
- 修复长页面观察挤掉原始任务、导致小型本地模型忘记“给谁发什么”的问题；历史压缩始终保留首条任务，格式不稳定或过早输入时会自动升级到已安装的 9B 视觉模型。
- 修复没有输入框时仍执行 `Type`、长按粘贴误触底部导航的问题；输入动作会先验证可编辑控件，发送完成还需动作成功、界面变化及收件人/正文证据闭环。
- 明确消息任务会优先使用目标应用的 Android 标准文字分享入口预填正文；端侧 OCR 先定位收件人，再在确认页同时核对收件人与正文，匹配后才点击“发送”，避免模型猜测微信页面坐标。
- 修复隐藏自身悬浮窗后截图协程可能永久等待的问题，并为屏幕捕获增加 10 秒超时。
- 修复本地视觉模型底层预填阶段不响应协程取消，超过单步时限后界面仍一直显示“正在识别界面并规划下一步”的问题；现在硬截止时间能及时结束任务，并通知底层停止生成。
- 修复语音助手快速回复与手机操作结果未写入会话、侧边栏找不到记录的问题；无障碍未连接时，手机操作进度会回退到系统悬浮窗继续显示。
- 提高声纹唤醒容错：降低默认声纹阈值，并允许较长唤醒词出现一次识别偏差。
- 补齐倒计时与秒表本地操作：支持开启、暂停、继续、查询、关闭 / 停止和重置，后台执行时不会抢占当前主屏。
- 恢复手机任务的无障碍自动启用：先启动目标应用，真正需要读屏时若 Murong 无障碍未连接，会在 Root 可用时自动合并启用并等待服务，不覆盖其他已启用服务；“设备与权限”页也恢复状态说明和手动触发入口。
- 修复主动取消后台手机任务时把协程取消误报成“任务失败：Job was cancelled”的问题。
- 修复 GUI 工具把取消异常包装成普通失败、导致聊天中出现 `StandaloneCoroutine was cancelled` 的遗漏路径；取消现在只终止任务，不再生成助手错误气泡。
- 修复搜索等手机任务结束后自动关闭离屏、用户无法返回查看的问题；无论成功、失败还是需要确认，只要离屏仍可用就会保留最终画面和入口等待用户验收，只有用户明确点“关闭离屏”或开始新的离屏任务才回收。
- 修复“全屏接管”通过重新启动应用入口导致商品页刷新或状态丢失的问题；现在迁移正在运行的任务栈，保持同一 task、Activity 实例、二级页面与滚动状态，迁移成功后再释放虚拟显示。
- 修复离屏任务已释放后查看器仍显示最后一帧、看似可点却无响应的问题；现在会立即清空旧帧并提示当前没有运行任务。用户可见进度不再显示只供系统内部使用的临时 display ID。
- 修复高分辨率 JPEG 帧超过 Binder 单次传输限制导致离屏空白或失败的问题，改用文件描述符管道传输；原机 `1440×3136` 画面可正常显示。
- 修复完成任务后从全屏查看器返回聊天时，小屏帧循环因启动竞态偶发提前退出、一直停在“正在连接”的问题；预览线程现在先登记生命周期再开始抓帧。
- 修复 Android 15+ 强制边到边布局下离屏标题与操作按钮侵入状态栏 / 挖孔区域的问题。
- 修复聊天框下方模型有时无法切换的问题（活动中继 model 未同步）。
- 修复项目页缺少新建文件 / 新建文件夹入口的问题。
- 修复编辑器“运行”按钮提示需要下载 python，但实际报 `exec: python: not found`（退出码 127）的问题。
- 修复 Agent 与编辑器用不上终端扩展或系统 Termux 里已安装 python 等环境的问题。
- 修复 Android release 包点击朗读闪退（R8 拆箱导致 JNI 反射 `invoke([F)Ljava/lang/Integer` 失败）的问题。
- 修复桌面端朗读把操作按钮文字一起读出来的问题。

### Security

- 语音模型与运行时均从官方 Release 下载并校验 SHA-256；录音只在本机识别期间存在，识别完成后临时音频立即删除，不上传。
- 无障碍只在用户明确发起需要读屏或点击的手机操作任务后尝试 Root 自动启用；纯打开应用不会触发，已有无障碍服务会被合并保留。
- Root 离屏桥只接受 UID 0、当前随机令牌和当前客户端 Binder；不向普通应用公开离屏服务记录，客户端退出后立即回收虚拟显示与辅助进程。

### Verification

- 本轮桌面主包 `go test .`、`go build .`、Wails Windows 正式打包、前端 `node --check` 与差异检查通过；计划解析测试覆盖“3 个顶层步骤 + 多个缩进说明”不再误拆，项目浏览测试覆盖进入二级 / 三级目录与越界拒绝，终端测试覆盖非零退出和 Git 非交互环境，三种会话导出测试覆盖凭据脱敏，发布工作流测试覆盖 VLM 中间产物与 Go 主模块隔离。
- Android `:core:testDebugUnitTest`、`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过；新增测试覆盖重复图片缓存副本去重、图片接口 400 友好提示不泄露原始响应，以及内部最终收口拦截只进入审计不进入用户气泡。Debug APK 已通过 ADB 覆盖安装到 RMX5200 并保留原数据，冷启动进入 `MainActivity`，日志未出现崩溃。
- ADB 真机本地 9B 模型回归中，聊天卡片先显示“当前处于拼多多首页，未找到”，随后原位扩展为完整段落，确认 token 按累计文本流式更新而非逐句覆盖；模型随后仍尝试点击左下角首页时，执行记录显示该 Tap 被底部一级导航证据拦截，拼多多页面没有变化。第二步纠错后的“当前处于拼多多首页，无需进一步操作”作为新段落追加，前两段模型原文仍完整保留。
- ADB 真机确认手机操作胶囊可点击展开、通过“—”收起，拖动到新位置后仍可再次展开；任务完成态保持“待确认”，没有自动关闭离屏。完成态 `dumpsys notification --noredact` 同时确认三个实际动作依次为“返回聊天 / 回到语音助手 / 离屏控制”，没有被替换为“确认并关闭”。模型输出状态测试覆盖同一步流式合并、跨步骤追加及预设心跳不混入真实模型文本。
- Android app 389 项与 core 703 项单元测试通过（合计 1092 项、0 失败），Debug APK 构建通过；最终真机验收态保留 Display 23（原机 `1440×3136`、60 Hz）及拼多多“oppo150w充电器”商品结果页，原始离屏截图已保存为 `app/build/outputs/phone-tests/pinduoduo-retained-final-v22.jpg`。物理主屏熄灭后，离屏、Murong 无障碍绑定与流体云仍保持可用。
- ADB 真机无人干预回归“打开 QQ 给雪绒的3号小屋发送你好”通过：Android 标准分享页预填“你好”，端侧 OCR 依次定位联系人并在确认页复核收件人与正文，自动点击发送；最终聊天页出现新消息气泡和发送时间。此前由人工点击产生的微信“你好”不再作为自动发送证据。
- ADB 冷启动离屏探针通过：日志确认使用 UID 0 的 Root 直连且未出现 Shizuku `:agentdisplay` 进程；原机 `1440×3136` 文件描述符画面可显示并触控，查看器顶部控件避开状态栏，`force-stop` 后 Root 辅助进程已自动退出。真机接管回归中，拼多多 `task #17289` 与 `NewPageActivity` 实例在 display 42 → 0 后保持不变，虚拟显示随后释放。完成态回归中，“打开拼多多搜索oppo150w充电器”自行闭环后通知保持 promoted ongoing 的“待确认”验收态，虚拟 Display 继续存在，流体云精确显示“返回聊天 / 回到语音助手 / 离屏控制”三个动作；点“离屏控制”可回看商品结果，返回聊天后内嵌小屏仍能重新抓取真实画面。运行中主动关闭的回归则确认通知与 Display 均被移除，聊天未新增 `StandaloneCoroutine was cancelled` 或 `Job was cancelled`。虚拟显示仍固定为 60 Hz，JPEG 预览帧率随分辨率与负载变化；主屏接管绕过 JPEG 链路并按物理屏原生刷新。
- ADB 回归“打开微信”：微信直接进入前台，通知显示“已打开微信。”，Murong 无障碍保持关闭；返回主界面后，语音手机任务已出现在会话侧栏。
- ADB 实机逐页确认抖音正确路径为“右上角搜索 → 输入极客湾 → 搜索结果 → 头像 / 用户名 → 跳过置顶 → 最新普通作品 → 灰色爱心”，点赞后爱心已变红；Murong 回归日志与聊天内嵌离屏均显示通过 `app-deep-link` 进入“极客湾”结果页，未点击页面上的“关注”。
- ADB 真机系统指令回归通过：手电筒开/关由系统相机服务确认，关闭自动旋转后 `accelerometer_rotation` 从 1 变为 0，打开相机后物理主屏进入 ColorOS 相机。
- ADB 真机逐页确认微信视频通话正确路径为“聊天 → 底部加号 → 视频通话 → 视频通话”，已实际拨出并由聊天中的“对方已拒绝”通话记录闭环；抖音正确路径为“消息 → 搜索联系人 → 私聊 → 顶部电话 → 视频通话”，已进入“等待对方接听”页后立即挂断。两项是为校准 Phone Agent 的人工 ADB 路径验证，不冒充本地模型无人干预完成。
- ADB 真机美团闭环已把区域切换到“张家港·德积街道”，进入“蜜雪冰城（德积店）”，选择冰鲜柠檬水（标准、正常冰、七分糖）并加入 1 杯购物车；页面显示 ¥5.8、仍差 ¥11.2 起送，按安全边界未提交订单、未付款。
- Desktop 主包与 runtime-packager 的 Go 测试、前端 `node --check` 和资源构建通过；Nuphus `initialize` / `tools/list` 实测返回 36 个工具并启用严格确认。现有 Windows TTS 的 `unsafe.Pointer` 静态检查警告仍由后续独立处理。
- GitHub Actions 将重新生成 1.37 全平台产物，把本文件同步到 GitHub Release 和后端更新说明，并分别上传 Android 主程序与终端扩展。
