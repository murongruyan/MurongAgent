# MurongAgent 1.27

## 1.27 / 26071704 — 2026-07-17

### Fixed
- 设置中的“设备权限”新增“文件访问”状态和“授权全部文件访问”入口。授权后，终端与 Agent 可访问 `/storage/emulated/0` 下的 ZIP、IMG、SH 等普通文件，而不再只显示目录。
- 未授权时，终端和 Codex 会说明共享存储受限，避免把被 Android 隐藏的文件误报为已检查。

## 1.26 / 26071703 — 2026-07-17

### Fixed
- Codex 不再预先占用一个空助手消息；工具先执行时，最终回答会正确排列在命令和结果之后。
- 本地 Codex 工具卡现在会保留命令中的实际文件路径，同时继续隐藏令牌、密码等敏感值。

## 1.25 / 26071702 — 2026-07-17

### Fixed
- Codex 的命令执行卡现在展示真实命令、工作目录、退出码、耗时和官方返回的可用输出。
- 过程卡在布局展开后会重新滚到末尾，最后一条命令结果不会卡在输入栏下方。
- 模型控制的二级弹窗跟随所点击的一级条目定位，速度菜单不会再偏到屏幕上方。

### Changed
- 点按额度圆环改为显示紧贴圆环的小浮层，点击空白处即可关闭，仍可手动刷新。

## 1.24 / 26071701 — 2026-07-17

### Fixed
- 响应速度菜单补回“默认（跟随官方模型）”，会真正清除自定义速度覆盖。

### Changed
- 聊天页额度移到输入栏旁的剩余额度圆环；点按才查看文本详情和刷新。

## 1.23 / 26071633 — 2026-07-16

### Fixed
- 模型总控先关闭一级菜单再打开二级菜单，修复部分投屏环境点击分类后无反应的问题。

## 1.22 / 26071632 — 2026-07-16

### Fixed
- 当 ChatGPT / Codex 的官方模型目录尚未返回时，推理深度与响应速度会以“待确认”状态保留在总控菜单，避免无提示隐藏或伪造模型能力。

## 1.21 / 26071631 — 2026-07-16

### Changed
- 聊天输入栏将后端、模型、推理和速度分层选择；官方模型目录从 app-server 动态读取，API / 中转连接与 ChatGPT / Codex 可直接切换。

## 1.20 / 26071630 — 2026-07-16

### Fixed
- ChatGPT / Codex 登录成功后，聊天页会自动使用官方后端，而不是继续显示并使用旧 API 模型。

### Added
- 聊天输入栏可快速切换“ChatGPT / Codex”与所有已配置 API / 中转连接；模型标签与实际后端同步。
- 聊天页显示官方 ChatGPT / Codex 的本周剩余用量、重置倒计时及刷新入口；数据仅来自 app-server 的只读用量接口。

## 1.16 / 26071626 — 2026-07-16

### Fixed
- 修复本机 HTTPS CONNECT 隧道错误缓冲 TLS 数据、导致 ChatGPT 设备码请求超时的问题；TLS ClientHello 和响应现实时透传。

## 1.15 / 26071625 — 2026-07-16

### Fixed
- Linux 静态 Codex app-server 现在使用终端扩展内置的 PEM 根证书包，并通过受限本机 HTTPS 隧道访问 OpenAI；不会降低 TLS 安全性。

## 1.14 / 26071624 — 2026-07-16

### Fixed
- 修复 Android 在 IPv6 回环与 app-server IPv4 代理地址不一致时，设备码 HTTPS 隧道未接通的问题。

## 1.13 / 26071623 — 2026-07-16

### Fixed
- 修复官方 Linux 静态 Codex app-server 在 Android 上无法可靠请求 ChatGPT 设备码的问题。
- 网络桥仅监听本机回环并限制 OpenAI / ChatGPT HTTPS CONNECT，TLS 不会被解密；邮箱验证码仍只在官方授权页输入。

## 1.12 / 26071622 — 2026-07-16

### Fixed
- 修复官方 Codex app-server 在 PRoot/Android linker 下因静态 `ET_EXEC` 报 `code 1` 的问题。
- ChatGPT 邮箱验证只在官方浏览器授权页完成，应用不接收或保存凭据。

## 1.11 / 26071621 — 2026-07-16

### Added
- 新增官方 Codex / ChatGPT 后端与设备码登录，使用 Plus / Pro 的 Codex 权益，不要求 API Key。
- Codex 的流式回复、工具进度、授权请求、取消及会话线程恢复已接入应用。
- 全局/项目规则、记忆、技能与会话目标通过官方 `additionalContext` 传递。

### Changed
- 设置页可切换 API Provider 与 ChatGPT / Codex 后端，并管理登录状态。

## 1.10 / 26071620 — 2026-07-16

### Fixed
- **扩展环境仍被判定不可用**: 绝对目标 symlink 在运行时被跳过后，不再进入必需文件集合，避免环境完整性检查把无法创建且无需创建的链接当作缺失文件。
- **兼容扩展包 1.4**: 旧扩展 manifest 即使仍包含 Termux 绝对 keyring 链接，也可以正常安装并启用其余工具链。
- **POSIX `[` 命令被误判**: 工具链命令白名单明确接受标准 `[`/`test` 入口，不再因此拒绝整个扩展 manifest。
- **脚本命令被替换为自指断链**: 当命令入口和已解包脚本是同一路径时保留原文件，仅为 native 命令创建 `bin/` symlink。
- **`pkg`/`apt` 只有入口却无法安装**: 扩展环境通过 PRoot 映射官方 Termux 编译期前缀，并继承正确的包管理器环境；`pkg install`、`apt`、`dpkg` 不再被诊断脚本禁用。
- **高 targetSdk 下 PRoot 终端循环退出**: 首个 Termux ELF 通过 Android system linker 加载，`PROOT_LOADER` 使用扩展 APK 内原生库，避免应用数据目录 W^X 限制导致 `Permission denied` 和 `code 1` 重建。
- **动态安装命令仍被 W^X 拒绝**: 启用 `termux-exec` linker 模式，使 Python、pip、clang 等由 `pkg` 安装到工具链目录后的命令可以直接执行。
- **APT 仓库公钥链接缺失**: 扩展提供的绝对 keyring 目标正规化为工具链内相对链接，恢复仓库签名校验。
- **包管理器覆盖的命令被还原**: 工具链完整性修复只重建缺失或断开的入口，保留 `apt/dpkg` 已安装或升级的有效文件。
- **升级主程序后 APT 后装包消失**: APT 可删除的 `.murong-keep` 目录占位文件不再参与工具链完整性判定，避免冷启动误删并重建整个工具链，保留 Python、pip、clang 等后装包。
- **系统终端放大后泄漏颜色码**: 系统环境使用 Android `sh` 时改用无 ANSI 的提示符，终端尺寸变化不再显示 `39;172m` 等残缺控制序列；系统 Bash 仍保留彩色提示符。

### Changed
- **模型命令工具支持双环境**: `shell` 新增 `system` 与 `extension` 选择；系统环境继续使用 Root `su`，扩展环境以应用 UID 进入 PRoot/Termux，`pkg`/`apt` 安装的文件可与页面终端共同维护。
- **模型命令工作目录稳定化**: 主代理的 shell 命令默认固定到当前项目作用域，不再受两个持久 Root 会话随机复用及历史 `cd` 状态影响，也可通过 `working_directory` 显式覆盖。
- **工具链链接处理一致化**: manifest 验证、安装、缓存校验和最终可用性判断统一只处理可在应用沙箱内解析的相对目标链接。
- **工具链安装指纹升级**: 安装语义更新为 `relocatable-v2`，现有缓存会自动重建并应用包数据库、keyring 与 PRoot 兼容层。
- **项目运行器接入扩展包环境**: 自动安装依赖及 Python/Node 等项目命令与交互终端使用同一前缀映射，动态安装的命令可直接运行。
- **扩展运行时要求升级**: 完整包管理器环境要求扩展 manifest 提供 APK 内 `proot-loader` 入口；不满足时不会误报为可用包管理器。
